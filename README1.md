# File Update Python Project

This project demonstrates a Python algorithm to open a file, read its content, update an allow list by removing specified IP addresses, and save the updated data back to the file.
